# Required metadata
sonar.projectKey=org.codehaus.sonar:pacbase-sonar-runner
sonar.projectName=Pacbase project analyzed with the Sonar Runner
sonar.projectVersion=1.0

# Paths to the directories containing the PQC reports (required)
sources=src

# Language must be specified, because default language is Java
sonar.language=pacbz